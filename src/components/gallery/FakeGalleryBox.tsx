import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FakeGalleryBox = () => {
  return (
    <View style={{ height: 100, width: 100, backgroundColor: "gray" }}>
      <Text>FAKE</Text>
    </View>
  );
};

export default FakeGalleryBox;

const styles = StyleSheet.create({});
